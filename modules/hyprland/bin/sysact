#!/usr/bin/env bash
# sysact - simple power menu for Hyprland/tofi
set -euo pipefail

options=" Lock\n󰒲 Sleep\n󱣻 Hibernate\n Reboot\n⏻ Shutdown"

choice=$(printf "%b" "$options" | tofi)

[ -z "${choice:-}" ] && exit 0

case "$choice" in
" Lock")
  dms ipc call lock lock
  ;;
"󰒲 Sleep")
  systemctl suspend
  ;;
"󱣻 Hibernate")
  systemctl hibernate
  ;;
" Reboot")
  systemctl reboot
  ;;
"⏻ Shutdown")
  systemctl poweroff
  ;;
*)
  exit 1
  ;;
esac
